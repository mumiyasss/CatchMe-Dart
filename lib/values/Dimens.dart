class Dimens {
	static const titleSize = 40.0;
	static const subtitleSize = 20.0;

	static const leftMarginTitle = 26.0;
	static const bottomMarginTitle = 0.0;
	static const leftMarginSubtitle = 10.0;
	static const bottomMarginSubtitle = 5.0;

	static const appBarHeight = 87.0;
	static const maxValidShiftForAppBarTitle =
			(leftMarginSubtitle * 2) + 240; // 240 - approx text length for sans-serif
}